/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options go here.
  // For a basic setup, it's likely just this:
  reactStrictMode: true,
};

module.exports = nextConfig;